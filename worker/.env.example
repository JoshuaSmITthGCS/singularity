# Copy to worker/.env on the host and fill in real values.
# .env is gitignored — never commit it.

NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

ANTHROPIC_API_KEY=
# Optional — these are already the cost-optimal defaults (see docs/PRICING.md).
# ANTHROPIC_MODEL=claude-sonnet-5
# ANTHROPIC_ESCALATION_MODEL=claude-opus-4-8

WORKER_ID=worker-prod-1
# Optional — defaults shown.
# WORKER_POLL_INTERVAL_MS=5000
# WORKER_CLAIM_TIMEOUT_MINUTES=10
